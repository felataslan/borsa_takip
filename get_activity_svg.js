/* eslint-disable @typescript-eslint/no-require-imports */
const fs = require('fs');
const lucide = require('lucide');

const activity = lucide.icons.Activity;
// The default Activity icon definition
console.log(activity);
